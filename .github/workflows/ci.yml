name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

# Cancel superseded runs on the same PR — saves CI minutes when a fixup
# commit lands while the previous run is still in flight. Push events on
# main are kept since each commit is a real merge.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  test:
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        # Node 26 currently fails on yargs (transitive c8 dep) due to
        # stricter ESM resolution. yargs hasn't shipped a fix yet
        # (https://github.com/yargs/yargs/issues). Re-add 26.x once yargs
        # publishes a Node-26-compatible release.
        node-version: [20.x, 22.x, 24.x]

    permissions:
      contents: read

    steps:
    - uses: actions/checkout@v7
    
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v6
      with:
        node-version: ${{ matrix.node-version }}
        cache: 'npm'
        
    - name: Install dependencies
      run: npm ci --prefer-offline --no-audit --no-fund

    - name: Run linter
      run: npm run lint

    - name: Run tests (with coverage via c8)
      run: npm test
